[project]
name = "nexor-simulador"
version = "0.1.0"
description = "Comparador de regimes tributários (Reforma Tributária / IBS-CBS)"
requires-python = ">=3.9"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
