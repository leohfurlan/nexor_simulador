# Módulo "Simulador Tributário" do Nexor Fiscal — stack alinhado ao nfse_hub
# (FastAPI + SQLAlchemy async + Jinja/HTMX).

# --- Web / API ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9      # parsing de formulários (Form)
jinja2>=3.1

# --- Config ---
pydantic-settings>=2.2

# --- Banco ---
SQLAlchemy>=2.0
aiosqlite>=0.20              # dev standalone (SQLite async)
asyncpg>=0.29               # Postgres async — integração read-only com o Nexor Fiscal
alembic>=1.13               # migrations (integração)

# --- Import de planilhas (Fase 3) ---
# pandas>=2.2
openpyxl>=3.1              # importação de arquivos XLSX/XLSM

# --- Testes ---
pytest>=8.0
httpx>=0.27                 # usado pelo Starlette TestClient
